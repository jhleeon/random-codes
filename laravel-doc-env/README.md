### Follow This-
1.create env file using this command on your terminal
`cp .env.example .env` </br>
2.Install\upate composer
`composer install` or  `composer update`</br>
3.create encryption key
`php artisan key:generate` </br>
4.Run the command on your terminal to serve
`php artisan serve`
